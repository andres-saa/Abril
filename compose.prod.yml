version: '3.9'
services:
  redis:
    image: redis:7-alpine
    container_name: Abril_redis
    env_file: ./db/.env
    command: ['redis-server','--appendonly','yes','--requirepass','${REDIS_PASSWORD}']
    volumes:
      - redis_data:/data
    restart: unless-stopped

  api_asistente:
    build: { context: ./backend/asistente, target: prod }
    container_name: Abril_api_asistente
    env_file: ./backend/asistente/.env
    depends_on: [redis]
    restart: unless-stopped

  api_socket:
    build: { context: ./backend/socket, target: prod }
    container_name: Abril_api_socket
    env_file: ./backend/socket/.env
    depends_on: [redis]
    command: ["gunicorn","-k","uvicorn.workers.UvicornWorker","-w","1","-t","1","-b","0.0.0.0:80","main:app"]
    restart: unless-stopped

  proxy:
    build: { context: ./nginx }
    container_name: Abril_proxy
    ports: [ '80:80', '443:443' ]
    volumes:
      - ./nginx/conf.d:/etc/nginx/conf.d:rw
      - ./nginx/proxy_params.conf:/etc/nginx/proxy_params.conf:ro
      - ./nginx/certbot/www:/var/www/certbot:ro
      - ./nginx/letsencrypt:/etc/letsencrypt:ro
    depends_on: [redis, api_asistente, api_socket]
    restart: unless-stopped

  certbot:
    image: certbot/certbot:latest
    container_name: Abril_certbot
    volumes:
      - ./nginx/certbot/www:/var/www/certbot
      - ./nginx/letsencrypt:/etc/letsencrypt
    depends_on: [proxy]
    entrypoint: sh
    command: -c "if [ -n 'api.asistente.arhook.com api.socket.abril.arhook.com' ]; then for d in api.asistente.arhook.com api.socket.abril.arhook.com; do certbot certonly -n --keep-until-expiring --webroot -w /var/www/certbot -d $$d --email \"andrew19f@gmail.com\" --agree-tos --no-eff-email ; done; fi; trap exit TERM; while :; do certbot renew -n --webroot -w /var/www/certbot --quiet || true; sleep 12h; done"
    restart: unless-stopped

volumes:
  redis_data:
